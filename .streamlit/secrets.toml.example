# Copy this to .streamlit/secrets.toml for local runs (gitignored), or paste
# these keys into the app's "Secrets" settings when deployed on Streamlit
# Community Cloud. Values come from the summary printed by aws/setup_aws.sh.

aws_access_key_id     = "AKIA..."
aws_secret_access_key = "..."
aws_region             = "us-east-1"
table_name             = "bmv080_pm_readings"
device_id              = "bmv080-01"
